<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Audio service (background playback) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Android 13+ notification permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Storage: nothing is read from the user's gallery.
         Downloads are written to app-private storage, and both open_filex
         and share_plus hand files to other apps through a FileProvider,
         which grants access per-URI without any permission at all.

         These removals are load-bearing, not tidying. Plugins in the
         dependency tree merge the media permissions into the final
         manifest, and Play then requires a Photo and Video Permissions
         declaration justifying access the app does not actually use —
         which blocks the release and cannot be answered honestly,
         because the honest answer is "we don't need them".

         If a future feature genuinely reads the gallery (a profile
         picture, say), delete the relevant line here AND complete that
         declaration in Play Console. Do not do one without the other. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:node="remove" />

    <application
        android:label="Know Thyself"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Deep links back into the app: password reset emails, and
                 the course-checkout web page once Razorpay confirms a
                 payment (so paying returns the user straight to their
                 course instead of leaving them stranded in the browser).
                 No autoVerify here — that is for http/https App Links and
                 does nothing for a custom scheme. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="meditationapp" android:host="reset-password" />
                <!-- host "app" so the destination lives in the PATH
                     (meditationapp://app/payment-success), which is what
                     go_router actually routes on — a host-only link
                     resolves to "/" and matches nothing. -->
                <data android:scheme="meditationapp" android:host="app" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
        </activity>

        <!-- audio_service: background playback notification -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Firebase Messaging: the channel a reminder lands in when the
             app is in the background and FCM posts the notification
             itself. Must match the channel_id the send-session-reminders
             function sets, and the channel PushService creates — if the
             three disagree, Android quietly files the notification under
             a default channel and the high-importance setting is lost. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="session_reminders" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Android 11+ (API 30+) restricts package visibility by default -
         without this, url_launcher can't see any browser app to open the
         external checkout link, and launchUrl silently fails. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
