name: ott_app
description: OTT mobile application (video/audio streaming)
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  supabase_flutter: ^2.5.6
  device_info_plus: ^10.1.0
  flutter_secure_storage: ^9.2.2
  crypto: ^3.0.3
  video_player: ^2.9.1
  wakelock_plus: ^1.2.5
  just_audio: ^0.9.40
  audio_service: ^0.18.15
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
